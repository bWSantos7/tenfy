{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "nixpacks.toml"
  },
  "deploy": {
    "startCommand": ". /opt/venv/bin/activate && python manage.py migrate --noinput && PLAYWRIGHT_BROWSERS_PATH=/app/.playwright celery -A config worker --loglevel=info --concurrency=4 --max-tasks-per-child=100",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
